# V6 方案：cell 搜索效率与搜索能力升级（按预期效果排序）

版本 v1.0 · 2026-07-14 · 依据 = r2 480ep 完整战役数据（`outputs/2026-07-13_v5r2_np2/{seg_lo,seg_hi}` 全程、`2026-07-13_v5r2_greedy` 至 ~ep250）+ 当前代码。
状态：**待批准，未动代码**。

---

## 0. 现状基线：架构与实测瓶颈

### 0.1 当前 cell 搜索架构（代码锚点）

- 结构变异**每轮必做**（`_outer_mutate` trainer/arith_das.py:1697，v1.1 决策：v1.0 把结构搜索强度砍到 40% 曾导致同误差功耗劣化 ~16%，**v6 一切改动不得触碰这条**）。
- 骰子只管 cell 叠加层，4 臂概率写死（:1714-1719）：keep / cell / resample / zero，实测占比 33/35/17/13-15%。cell 臂内部还有一层写死的子骰子：add 50% / remove 25% / swap 25%（:1736-1744）。
- zero 臂 = 整列清零（`_op_zero_col` :1642），子代误差门 = `archive.hi`（`_outer_tt_oracle_screen` :1874、`_gate_budget_exceeded` :1769）——只拦出档案范围，不拦越箱。
- 每 episode 求解器只出 **1 个** cell 配置，33 路 DC 样本全部共用、只变布线（`get_samples` :3412、鲁棒修复 `_outer_greedy_solve_robust` :1991）。
- 种子 = 整数 k 的纯 Dadda 队列（`enable_pareto_v5` :4261）；档案 = MRED log 等比箱 × 每箱 8 个非支配点（utils/common.py:131）。

### 0.2 r2 480ep 实测瓶颈（全部可复算，脚本在 scratchpad/analyze_*.py）

| 证据 | 数字 |
|---|---|
| 产出衰减 | admit/40ep：seg_lo 90→~10（ep200 后）、seg_hi 73→~15；greedy ep120 后 ≈0 |
| 停滞时点 | 每箱最后 >1% 改进中位：seg_lo area **ep79** / power ep99（后 380ep 近零产出）；seg_hi 379/279 |
| op 账本 | cell 臂 35% episode → 32-35% 入档（主力）；keep 臂 **33% 预算 → 20-22% 入档，性价比垫底 0.40-0.45/ep**；zero 臂全场性价比反而最高 0.70-0.74/ep |
| zero 臂失控 | 目标箱命中 7-19%、over_budget 85-86%、入档漂移 +0.78 箱 → **~13% DC 预算被虹吸到越箱后代** |
| 空间欠挖 | 终局低误差箱设计只有 0-4 个 cell（菜单塞不进）；终局前沿 1/3 是 zero+substd 混合体，但只能靠两臂碰运气接力才产生 |
| 终局战绩 | 对紫线（单目标 240ep/点 专攻的纯截断 arith）双轴支配仅 **3/10**（k06/k12/k18）；钉子户 k08/k10/k16（面积 +4.8~+9.4%） |

**三个瓶颈**：① 假设吞吐——每 33 次 DC 只检验 1 个 cell 假设；② 邻域淘干——单亲本单步变异、无杂交无迁移，ep200 后全臂产出跌 3-4×；③ 配比失配——骰子概率写死，keep 超配、zero 不看箱位置。

---

## 1. 排序总表

排序口径 = **预期增益 × 置信度**（增益估算见各方案正文；置信度=数据直接支撑程度）。

| 排名 | 方案（原编号） | 预期增益 | 置信度 | 改动量 | 依赖 |
|---|---|---|---|---|---|
| **R1** | 单 episode 测多 cell 假设（原五） | 后半程 admit ×1.5~2.5，天花板最高 | 高 | 中 | 无 |
| **R2** | bandit 自适应骰子·按箱条件化（原一） | 全场 admit +10~15% + 低箱 zero 自动归零 | 高 | 小 | 无 |
| **R3** | 档案内 GA 杂交（原二） | 专治 ep200+ 平台期，若达 cell 臂半产出则 +~15% | 中 | 中 | 无 |
| **R4** | 跨箱移植 M3（原三） | 定点攻 3 个钉子户，目标转化 ≥2 个 | 中 | 中 | R3 的槽位坐标基建 |
| **R5** | zero 渐进化+定向门（原四） | 回收被虹吸的 ~13% DC（天花板即此） | 高 | 小 | 无 |
| **R6** | 零列阶梯种子族（原七） | +3~5%（种子期高产 2.8-4.2/ep 的外推） | 高 | 小 | 无 |
| **R7** | cash-in 打分升级+菜单可用性表（原六） | 探针后定（低箱拒绝率>50% 才显著） | 低 | 小中 | 先上探针 |
| **R8** | 60ep op-ablation 对照（原八） | 不直接增益；为 R2/R3 提供因果实锤 | — | 小 | 无 |

排序理由一句话：R1 攻击的是**约束里最紧的那条**（假设吞吐），且不加一分钱 DC；R2 便宜、确定、立刻生效；R3/R4 天花板高但有工程不确定性；R5/R6 收益确定但天花板明确；R7 证据不足先探针；R8 是保险。

---

## 2. 方案详情

### R1（原五）单 episode 测多个 cell 假设 ⭐ 排名第一

**机制**：`struct+cell`/`resample`/`zero`/杂交轮次中，求解器/算子产出 **top-G 个多样配置**（G=4，温度化贪心打分 + 随机破平局；zero 臂 = 不同清零行数的 G 档），33 路样本按 G 组均分（8+8+8+9），组内共用配置、只变布线。PPO advantage 在**组内**归一化后再合并。
**数据依据**：keep 臂（纯布线重采样）产出垫底且持续衰减 → 33 路布线对单配置是过度采样；cell 假设才是稀缺品。同样的 DC 预算，假设吞吐 ×4。
**伪代码**：
```
configs = solver.top_diverse(G=4, temperature=τ)   # 或 zero 臂的 4 档行数
for g, cfg in enumerate(configs):
    apply(cfg); samples[g] = sample_routing(n=33//G)
adv = concat([normalize(adv_g) for g in groups])    # 组内归一化
admit(all samples)                                  # 档案照常逐样本准入
```
**改动点**：`get_samples`（:3412 分组循环）、`_outer_greedy_solve_robust`（:1991 返回 top-G）、`get_ppo_loss`/advantage 归一化（:3832 按组）、日志 admit 行加组标签。
**验收**：cell 类 episode 的 admit/ep ≥ 1.5×r2 同期；keep 臂对照产出降幅 ≤20%（布线不崩）；PPO loss 无 NaN。
**风险与回退**：每组布线样本变少 → 布线质量降。回退开关 `outer_multi_config=1` 即回单配置。G 从 4 起步不上 8。

### R2（原一）bandit 自适应骰子（按箱条件化）

**机制**：每个 `(箱, 臂)` 维护滑动窗口（近 12 次）admit 计数，Thompson 采样选臂，每臂保底 5%。臂集合 = {keep, cell, resample, zero, crossover(R3 上线后)}；cell 子骰子（add/remove/swap）同机制。**结构必做层不动**。统计随 `front_state.json` 持久化（save_experiment :2389 处并入）。
**数据依据**：keep 33% 预算 ↔ 20% 产出；zero 在 seg_hi 尾箱是后半程冠军（0.63/ep）、在低箱必越箱——好坏强依赖箱位置，静态概率必然全局次优。估算：keep 33%→~15%，移出的 episode 按 cell/zero 产出（0.7~1.17 vs 0.40）折算 ≈ 全场 +10~15%。
**改动点**：`_outer_mutate`（:1714-1719 概率来源改为 bandit 查询）、新增 ~60 行统计类、front_state 序列化。
**验收**：跑 100ep 后 keep 臂自动占比 ≤20%、低箱 zero 占比 ≤5%；同 ep 对比全场 admit ≥ +10%。
**风险与回退**：产出非平稳（箱饱和）→ 滑动窗口已覆盖；`outer_bandit=0` 回静态概率。

### R3（原二）档案内 GA 杂交

**机制**：新臂 `crossover`：同箱抽两个亲本（拥挤度加权，偏取相异者），**结构取 A**；cell 配置均匀杂交——A∩B 槽位随机继承一方类型，A 独有槽位保留。子代过 TT-oracle（免费），超预算走 `_outer_greedy_solve_robust` 贪心修复。保守版先限"同 k 亲本"。
**数据依据**：每箱存 8 个非支配个体从未重组；终局 1/3 前沿是 zero+substd 混合体（现在只能两臂接力碰运气）；ep200+ 全臂 0.1-0.4/ep 的平台期正是教科书上交叉的适用场景。若杂交臂后半程做到 cell 臂前半程的一半（~0.5/ep）即净增 ~15%。
**改动点**：`_outer_mutate` 新分支、槽位对齐工具（slot 坐标 = assignment 顶点身份，同 k 下天然可对齐）、亲本对采样。
**验收**：后半程（ep240+）杂交臂 admit/ep ≥0.5；其产出中混合体占比 ≥30%。
**风险与回退**：异构亲本槽位错位 → 先同 k；修复失败率高 → 修复不动点直接弃样（TT 免费不花 DC）。

### R4（原三）跨箱移植（M3 完成版）

**机制**：cell 配置改写为**相对截断边界的坐标**（第 k+j 列 → 类型），把箱 B 在位者的图案提升到箱 A 的 k' 上重实例化（k′±1 相邻优先），TT 预筛后作为 A 箱注入候选；配合 k± 宏变异（seed k 整体平移）。
**数据依据**：尾箱握着 −10% 功耗的零列方案，中段箱看不见；钉子户 k08/k10/k16 的相邻箱都有更强图案。每箱独立摸索是 r2 停滞的结构性原因之一。
**改动点**：相对坐标编解码（复用 R3 的槽位工具）、`_v5_begin_episode` 注入路径。
**验收**：k08/k10/k16 至少 2 个转为双轴支配紫线。
**风险**：图案跨 k 不可行（列高不同）→ TT+修复兜底，弃样零成本。

### R5（原四）zero 渐进化 + 定向门

**机制**：`_op_zero_col` 清零行数从"整列"改为采样 1~n 行（几何分布偏小）；zero 子代的误差门从 `archive.hi` 收紧为 `min(archive.hi, parent 箱上沿 × 4)`（允许越 ~2 箱，禁止跳尾）。
**数据依据**：85-86% over_budget、7-19% 命中、+0.78 箱漂移 → ~13% DC 虹吸（回收上限即此数）。
**改动点**：`_op_zero_col`（:1642）、`_gate_budget_exceeded`（:1769 加 parent 参数）。
**验收**：zero 臂 over_budget ≤40%、目标箱命中 ≥40%。
**风险**：门太紧压制尾箱产出 → 系数 4 可调；`outer_zero_gate_mult=0` 回旧行为。

### R6（原七）零列阶梯种子族

**机制**：种子队列从整数 k Dadda 扩为确定性族：每对相邻 (k, k+1) 之间插 2 档"Dadda@k + 最低列清 m 行"（m 取列高 1/3、2/3），构造规则写进论文附录保证可复现。
**数据依据**：种子 episode 全场性价比最高（2.8-4.2 admit/ep）；零基因现在要等骰子中彩票才进箱；半格种子同时填 mred 空隙。约 +20 个高产 episode ≈ 全场 +3~5%。
**改动点**：`enable_pareto_v5` 种子生成（:4261）、种子构造器 ~40 行。
**验收**：种子期结束非空箱 = 全部箱；半格种子在后续变异中被选为亲本 ≥30%。
**风险**：种子期变长挤占进化预算 → 档满 (n_ok>0) 即出队的现行机制天然限制。

### R7（原六）cash-in 打分升级 + 菜单可用性预表

**机制**：先上**探针**（一行日志：cell 提案被误差门拒绝的比率，按箱统计）。若低箱拒绝率 >50%：打分从 Δarea 改为 Δarea/(wae·2^(col−k))（单位误差预算的面积收益），并按箱预算预计算"该箱该列哪些 cell 根本不可行"直接跳过；zero cell 打分弃用标称 0 面积、改用 `_EXACT_AREA_INCTX` 锚点的真实边际。
**数据依据**：低箱终局只有 0-4 个 cell——但"提案被拒"还是"提案赢不了在位者"未实证，先探针后动手。
**验收**：探针数据成立后上线；上线后低箱拒绝率 ≤25%，低箱 cell 数中位 +1 以上。

### R8（原八）60ep op-ablation 对照

**机制**：v6 启动前三条短线（同种子同预算 60ep）：全静态骰子 / 只 cell 臂 / R2 bandit。
**产出**：验证"骰子多臂有必要 + bandit 优于静态"的因果结论；若 bandit 不显著优，R2 降级为纯 keep 降权。
**成本**：3×60ep ≈ 半天（32 并发）。

---

## 3. 实施分期（按依赖与改动量，标注排名）

| 期 | 内容 | 预估工时 | 出口条件 |
|---|---|---|---|
| Phase 0（可选） | R8 ablation 三短线 | 0.5 天跑 + 0.5 天改 | bandit 因果结论 |
| **Phase A 最小包** | R2 bandit + R5 zero 渐进/门 + R6 种子族 + R7 探针 + 既有审查项（seed 出队语义修复、no-mred 监控） | 1~2 天 | 冒烟全绿 + 60ep 试跑验收各自指标 |
| **Phase B** | R1 多配置 episode + R7 打分（若探针支持） | 2~3 天 | R1 验收达标 |
| Phase C | R3 杂交 → R4 移植 | 3~4 天 | R3/R4 验收达标 |

崩溃补丁包（审查清单 P1-P3：full-PPA 隔离、isfinite 门、空档案早退）随 Phase A 一并带上——r3 启动前必修。

## 4. 战役级验收（r3 vs r2，同预算 480ep）

1. 后半程（ep240+）admit/40ep ≥ 2× r2 同期（seg_lo ≥20、seg_hi ≥30）；
2. 双轴支配紫线 k 点 ≥ **6/10**（r2 = 3/10），其中 k08/k10/k16 至少 2 个；
3. 中段箱（seg_hi bin3-7）箱内最优 area 全部 ≤ r2 终局；
4. zero 臂目标箱命中 ≥40%（r2 = 7-19%）。

注：中低箱**功耗轴**在环内 DC 口径下受仪表盲区限制（结构性差异 ±1% 不可见），本文档不承诺 DC 口径功耗指标；功耗轴的真实战绩以 XA 终审为准，仪表升级另立方案（见 §5）。

## 5. 范围外关联项（另立方案，不在本文档验收内）

- **功耗仪表**：learned power proxy 重校准（`use_power_proxy` 基建 + `enrich_features.py` 13 维特征）+ admit 触发式 XA 复评（入档 ~0.3-1 个/ep，分钟级可负担）→ 让功耗轴可搜。这是"双轴稳稳碾压"的另一半，优先级与 Phase B 同级。
- **greedy 线处置**：ep120 后产出 ≈0（确定性收敛 + zero 被求解器抹掉），建议 r3 起停开或仅作 R8 对照臂。
- **bin 调度 bandit**：与 R2 同一基建，可顺手做（饱和箱降频），不单独立项。

---
*变更记录：v1.0 初稿（2026-07-14），依据 r2 战役终局数据与双审查结论。*
