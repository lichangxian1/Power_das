# V7 方案：两阶段搜索 —— 先遗传定 cell，再优化连线

> 2026-07-17 起草。动机来自用户提议："先遗传确定完 cell，再优化连线"。
> 本文档 = 修改方案 + 基于历史训练数据的优缺点论证 + 实施步骤。**获确认后再动代码**。

---

## 1. 现状：cell 与连线在当前架构（v6.1）中如何耦合

先把事实钉死（file:line 以 trainer/arith_das_v5/ 为准）：

| 层 | 是什么 | 谁决定 | 每集吞吐 |
|---|---|---|---|
| 结构层 | 每列 FA/HA/CT42 计数（ct32/ct22/ct42） | 每集无条件随机 transition 变异（outer_search.py:346） | 1 假设/集 |
| 近似 cell 层 | state["cells"]，每槽放 exact 还是哪种近似 cell | bandit 臂（keep/cell/resample/zero/crossover），R1 多配置 G=4 | **4 假设/集** |
| 连线层 | samples_connection，每个源输出接到下一级哪个节点哪个端口 | GCN 双线性打分 + masked categorical 采样（rtl_emit.py:295, sampling.py:58），PPO 只学这一层 | 32 采样/集（4 配置各摊 8 个） |

三个关键事实：

1. **连线从不被遗传。** 档案父本只传 ct + cells（pareto_front.py:272 只 deepcopy ct），connection 每集由 GCN 全新重采。"好连线"唯一的存活方式是间接沉淀在共享策略权重里——而策略同时服务所有 k、所有箱、所有配置，信号被稀释。
2. **admit 判决混淆了 cell 好坏和连线运气。** 一个配置只分到 8 条连线采样；好配置可能连抽 8 条烂线而被埋没，反之亦然。bandit 归因（你刚修的 arm_ok）仍受这层运气污染。
3. **连线影响误差，不只影响 PPA。** 哪些信号喂进近似 cell 由 connection 决定，TT oracle 按整图（含连线）算误差。所以 cells×连线严格说**不可分**——这是本方案最大的理论风险，后面专门论证。

## 2. 历史训练证据（为什么值得改）

- **cell 假设吞吐是已被证实的第一约束。** v6.1 的 R1（G=1→4）是全部升级里收益最大的一项：cell 臂入档产出提升 ~3.5×。而 Monte Carlo 仿真证明 bandit 分配已达 oracle 的 97%——瓶颈不在"选哪个臂"，在"每集能测几个配置"。
- **连线深度值 30–50µm²。** 面积缺口分解实验把 v6.1 前沿与纯截断的差距归因为：进化出的次优分数形态 + 连线/路由深度 ~30–50µm²（k12 量级约 5–8% 面积）。这是"专门优化连线"能兑现的池子。
- **持续训练策略的边际收益很小。** lr=0 冻结策略对照（2026-07-09_23）vs 同期 PPO（2026-07-09_21）：k06 900.3 vs 879.1（+2.4%）、k12 700.6 vs 692.8（+1.1%）、k18 406.1 vs 403.0（+0.8%）。对照集数更少，此差值取上界看待。结论：**温启动策略"冻住当路由器"损失 ≤1–3% 面积**——Phase 1 冻结策略的代价可控，且幸存者会在 Phase 2 被重新精修。
- **用户自己的观察：**"新的方案（v6.1 温启动）后续好像并没有提高它的搜索能力，几乎和它温启动的结果一样"——策略后期持续学习本来就近乎停滞，Phase 1 放弃它损失很小。
- **无箱档案已验证更优**（2026-07-17 ep119 严格同 ep 对比：seg_hi 面积 6 胜 3 负，band 8 −10.3%；淘汰启动 27 集无侵蚀）。V7 直接建在无箱档案上。

## 3. 方案总览

```
Phase 1（~240ep）：GA 定 cell           Phase 2（~200ep）：冻 cell 优化连线
┌────────────────────────────┐         ┌────────────────────────────┐
│ 连线：温启动策略 argmax 定线  │         │ 从 P1 档案选精英 ~10-20/段    │
│ （确定性，全配置共用口径）      │   →     │ 每精英：结构+cells 冻结        │
│ 每集 32 样本 = 32 个 cell 配置│         │ 32 样本全是该配置的连线采样     │
│ （G=32，吞吐 8× 于 v6.1）    │         │ PPO 开启，集内 advantage 归一  │
│ PPO 关（lr=0），纯 GA+bandit │         │ 每精英 ~8-15 集，留最优连线     │
│ 无箱 3 目标档案               │         │ （可选）ufomac MILP 路由对照   │
└────────────────────────────┘         └────────────────────────────┘
                                        → 汇入档案 → 终局前沿 → XA 终审
```

## 4. Phase 1 详细设计：cells-only GA

**核心改动：G 从 4 提到 num_samples（=32），连线退化为确定性口径。**

- 每集流程不变（选父本 → 结构变异 → 臂变异），但被选臂掷 **32 次**去重收集 32 个 cell 配置（现在是 4 次）；32 条采样每条对应一个配置（`sample_idx % G`，G=32 即一一对应）。
- 连线用**温启动策略 argmax 解码**：`sample_from_logits` 加 greedy 标志，`Categorical.sample()` → `argmax`。同一结构下所有配置拿到同一条确定性连线 → 配置间排序不再受连线运气干扰（口径公平，哪怕这条线不是最优）。
- **PPO 关闭**（lr=0 且跳过 update；G=32 时组内只有 1 样本，advantage 归一退化为除零，必须跳过而非硬算）。
- TT 门控从每组首样本（4 次/集）变为逐配置（32 次/集）。缓解：先用已有的解析贡献 `wae·2^col` 预筛，TT oracle 只跑预筛通过者；或对门控总次数设上限。
- 档案：无箱（`--pareto_binless`），bandit 归因照旧（arm_ok 逻辑不动，且此后每次 admit 精确对应一个配置——归因比 v6.1 更干净）。
- 结构层吞吐仍是 1/集（32 配置共享同一 comp_graph，这是构图机制的硬约束，不动）。

**代码触点：**
- `outer_search.py` `_outer_mutate`：G 参数化到 num_samples；臂掷 G 次收集去重配置（现有逻辑改上限即可）。
- `sampling.py` `sample_from_logits`：加 `greedy=True` 分支（argmax 替代 sample）。
- `training.py`：phase1 标志下跳过 PPO update（不是只靠 lr=0）。
- `scripts/train_dc.py`：`--phase1_cells_only` 开关（隐含 G=32、greedy 连线、跳 PPO）。
- `environment.py` `_refresh_episode_cell_groups`：确认 G=32 无硬编码上限。
- 冒烟：`smoke_pareto_v5.py` 加 T11（G=32 分组正确、argmax 确定性可复现、PPO 跳过、组=样本一一对应）。

## 5. Phase 2 详细设计：routing-only 精修

**核心改动：把"种子集不变异"机制推广成"精英重放"。**

- 精英选取：从 Phase 1 终局档案按 log2 带取每带最优面积 + 最优功耗 + 次优（防错排），每段 ~10–20 个。
- 每个精英一批集：复用 `_v5_state_override`（无箱父本注入机制，现成）把精英的 ct+cells 直接写入 state，**跳过结构变异和臂变异**（类似 `_v5_seeding` 的"不变异"路径）；32 样本全部是该配置的连线采样；PPO 开启，advantage 集内归一（回到 G=1 语义，现有代码原生支持）。
- 误差照常逐样本 verilator 实测（连线变→误差变，admit 以实测为准，不信 Phase 1 的旧值）。
- 每精英跑 ~8–15 集后轮换；admit 回同一无箱档案（3 目标支配自动裁决新旧连线优劣）。
- （可选加菜）每精英额外发射一条 `ufomac_router`（utils/compressor_tree.py:563，MILP 最小 makespan，代码现成但从未接入）作为确定性对照候选——一次 DC 测量的成本换一个 arrival-aware 路由样本。
- **代码触点：** `pareto_front.py` `_v5_begin_episode` 加 phase2 分支（从 `--phase2_elites <front.json>` 轮询精英）；`scripts/train_dc.py` 加开关；导出/XA 流程复用现有。

## 6. 优点（对应证据）

1. **cell 假设吞吐 8×**（4→32/集）：直接放大已被证实的第一约束，R1 的 4× 已兑现 3.5× 产出，本方案是同一逻辑推到上限。
2. **排序去噪**：配置间共用同一确定性连线口径，admit/bandit 归因不再被连线运气污染（正面呼应你刚修的 arm_ok）。
3. **连线预算集中**：精英配置从 ~8 条采样 → ~300+ 条（8–15 集 × 32），针对 30–50µm² 的连线深度池子有真实兑现空间。
4. **连线首次可被保留**：Phase 2 最优 connection 连同配置存档（payload 本就存 connection，导出路径现成）。
5. **不增加墙钟成本**：每集 DC/verilator 样本数不变（32），Phase 1 纯粹是把同样的测量预算换个分法。

## 7. 风险与缓解（对应证据）

1. **cells×连线不可分**（最大理论风险）：argmax 连线下的最优 cell 配置 ≠ 精修连线下的最优。误差本身依赖连线（近似 cell 吃哪些信号）。缓解：(a) Phase 2 逐样本实测误差重新裁决；(b) 精英取每带 2–3 个含次优，给"需要好连线才发光"的配置留活路；(c) 门控只按预算砍、不做排序淘汰，保留预算内全部幸存者。
2. **冻结策略质量天花板**：lr0 数据表明代价 ≤1–3% 面积，且只影响 Phase 1 排序噪声，幸存者会被 Phase 2 重测。
3. **TT 门控成本 ×8**：解析预筛 + 门控上限缓解；若 pilot 显示单集超时，降 G 到 16（吞吐仍 4×）。
4. **精英错选**：Phase 1 排序有偏时 Phase 2 精修错对象。缓解同 1(b)，且两阶段共用档案，Phase 1 条目不删除、只被支配淘汰。
5. **组大小=1 的退化路径**：advantage 归一除零、组内 TT 门控语义变化——必须冒烟覆盖，不能只靠 lr=0 兜底。

## 8. 实施步骤

1. Phase 1 代码（G 参数化 + greedy 解码 + 跳 PPO + 开关）+ 冒烟 T11。
2. **60ep pilot**（单段 seg_hi，无箱，温启动策略冻结）：验收 = admit 经济学（入档/集 ≥ v6.1 同期 2×）、单集墙钟不超 v6.1 的 1.3×、无退化路径崩溃。
3. pilot 通过 → Phase 1 正式 240ep ×2 段（参数呈报确认后发射，~/Power_das 启动）。
4. Phase 2 代码（精英重放 + 开关）+ 冒烟 T12，在 Phase 1 跑期间完成。
5. Phase 2 ~200ep → 终局前沿 → XA 终审 → 与 v6.1 无箱基线同口径对比出裁决报告。

## 9. 决策点（2026-07-17 已裁决）

- **D1 Phase 1 连线口径：固定规则连线（用户提案，实验验证通过，见 §10）。**
  实现 = 逐 slice Kuhn 二分图匹配的规范化布线（行序列序固定 → 每个结构唯一确定一条连线，
  零策略参与）。注意：**纯首适配贪心会撞死角**（某些源只有唯一合法去处会被抢占），
  必须用带增广路径的匹配算法。argmax 策略解码保留为 pilot 可选 A/B 旗标。
- D2 Phase 2 优化器：PPO 精英重放 + ufomac MILP 对照（用户已确认）。
- D3 G 值：32，一样本一配置（用户已确认）；TT 门控超时则退 16。

## 10. 固定连线口径验证实验（2026-07-17）

**设计**：从 v61cold 480ep 终局档案两段各选 6 个配置（误差分位 + 最多 cell 条目），
每配置双发射：p=档案里策略采样的获胜连线，f=Kuhn 规范化固定连线；
同口径 verilator（seg_lo 16M / seg_hi 4M）+ DC@1.5ns 实测。24/24 成功。
忠实性 sanity：seg_lo p 变体 mred 与档案值逐位一致。

**结果（f 相对 p，n=12 配对）**：

| 指标 | 中位 | 均值 | 范围 |
|---|---|---|---|
| Δarea | **+1.5%** | +1.9% | −1.7% ~ +7.3% |
| Δmred | +15.6% | +38.7% | **−36.8% ~ +118.9%** |
| Δpower | +0.4% | +2.3% | −5.4% ~ +17.3%（小面积高 k 处波动大）|
| area 排名 Spearman | **+1.000（两段均满分）** | | |

**解读**：
1. 面积口径极好：固定连线只付 ~2% 均匀税，且 12 个配置的面积排序与策略连线口径
   **完全一致**——Phase 1 用固定连线选 cell 配置是可信的。税是均匀的，不扭曲选择，
   只让绝对值保守偏移（对比 lr0 对照的 1–3%，量级吻合）。
2. 误差是大变量：中位 +16%，多配置翻倍（+100% = 整跳一个 log2 带），也有两例
   误差反而降 21–37%。含义：(a) Phase 1 的 (配置, 固定线) 是真实设计、实测误差，
   档案完整性无损，但落带保守；(b) **连线本身是显著的误差优化杠杆**——现架构只靠
   每配置 8 条采样的 admit 运气兑现它，Phase 2 的连线精修不只回收 30–50µm² 面积，
   还能在同硬件下压误差（左移跨带 → 支配）。
3. 残余未知：策略 argmax 解码口径未实测（介于 p 与 f 之间），以及 n=12 下近平局
   （<2% 面积差）的配置可能翻序——档案每区多留条目可兜底。
