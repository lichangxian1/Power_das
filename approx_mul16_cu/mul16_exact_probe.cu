// ============================================================================
// mul16_exact_probe.cu -- 排查探针, 不是近似乘法器!
// 精确乘法, 接口与库里其他文件完全一致: int32_t int_mul(int32_t, int32_t)。
// 用它跑出的误差应与"不勾 Approximate Computing"的基线一致;
// 若不一致, 说明问题在工具的近似计算路径, 与乘法器文件无关。
// ============================================================================

namespace app {
#include <cstdint>

__device__ int32_t int_mul(int32_t a, int32_t b)
{
    return a * b;
}
}  // namespace app
