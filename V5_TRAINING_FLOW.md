# v5 多目标训练流程详解（现行 · 2026-07-13 r2 起跑）

对应正在运行的战役 `outputs/2026-07-13_v5r2_np2`（GA 两段）与
`outputs/2026-07-13_v5r2_greedy`（greedy seg_hi 单段 A/B）。r1（同日早间）因双评审
发现"恒零 cell 不在菜单 + group Z 检测错"（zero-col 实际不是分数截断）等 10 项漏洞，
修复后冷启动为 r2；r1 数据保留作对照。本文描述的 zero-col 语义自 r2 起才真实成立。本文把一个 episode 从头到尾拆开，
并说明战役宏观结构、三个测量口径、三个搜索维度如何协同。术语首次出现附中文解释。
**逐函数级的 episode 拆解（带代码行号、失败路径速查、完整术语表）另见
`V5_EPISODE_LIFECYCLE.md`**；本文管"为什么这么设计"，那篇管"代码里每步干了啥"。

---

## 0. 一句话概括

**一次训练直接产出覆盖宽 MRED 范围的 (误差, 面积, 功耗) 帕累托解集**：误差轴由"不同
截断深度 k + 分数截断 ZERO"张开，面积轴由压缩树结构 + 近似 cell 搜索优化，功耗轴由
真实 DC/XA 测量揭示；一个解能否存活**只由支配关系决定，不经过任何人工权重系数**。

（MRED = Mean Relative Error Distance，平均相对误差；帕累托解集 = 一组互不支配的解，
即没有任何一个解能在所有目标上同时优于另一个。）

---

## 1. 战役宏观结构

### 1.1 两个误差段进程（并行、独立）
gpu0 上同时跑 2 个 `train_dc.py` 进程，各自负责一段 MRED 区间：

| 进程 | MRED 区间 | 箱数 | 种子截断深度 k | 兜底 trunc_cols |
|---|---|---|---|---|
| seg_lo | [1e-7, 1e-3] | 14 | k2–k14 | 8 |
| seg_hi | [2.5e-4, 2e-1] | 10 | k12–k24 | 16 |

两段在 [2.5e-4, 1e-3] 重叠约 2 箱——重叠区两段各自搜出的解在最终合并时对撞，
互相校验、取并集后的非支配前沿。进程间**不共享内存/档案**（文件同步太复杂不值），
各跑各的，人工合并。

档案容量上限 = 箱数 × bin_cap：seg_lo 14×8 = 112、seg_hi 10×8 = 80，合计 192。
实际稳态占用远低于上限（ep400 时两段合计约 36 条）：支配准入只留非支配解，加上
功耗轴 1% 的测量噪声容差把"差不多的点"都挡在门外——箱全部非空但大多只有 1~2 条，
拥挤度淘汰几乎不触发。

### 1.2 每个进程持有什么
- **一个 GCN 策略网络**（Graph Convolutional Network，图卷积网络）+ 三个类型头
  （type head，给 3:2/2:2/4:2 压缩器槽位打 cell 类型分的线性层）；
- **一个 ParetoArchive**（非支配档案，见 §4.D）——本段的全部前沿解就存在这里；
- **一个种子队列**（本段的 k 列表，逐 episode 弹出一个做初始种群）；
- 独立的 32 个 DC worker（工作子进程，各自独占一路 DC 综合任务）进程池。

### 1.3 关键参数（本战役）
`ep=480`（每进程跑 480 个 episode；episode = 集，一轮"取亲代→变异→采样→评测→
更新"的完整循环）、`samples=32`（每 episode 采 32 个布线；布线 = 压缩器各输入
端口接哪些信号的具体连接方案，同一棵树不同布线 PPA 可差不少）、
`n_processing=32`（32 路并行 DC）、`bin_cap=8`（每箱最多留 8 个非支配解）、
`seed=42`（随机种子，固定后整个 run 可复现）、时钟 1.5ns、菜单 = 统一 substd
（sub-standard，自制近似压缩器 cell 库；22/32/42 三族全部 PPA < 标准单元。
PPA = Power/Performance/Area，功耗-性能-面积三指标的统称）。

---

## 2. 三个测量口径（整个流程的地基）

近似电路的每个数字都必须说清是哪个口径测的，否则不可比。

- **verilator（误差）**：verilator 是开源 Verilog 仿真器。每个候选设计发射成 RTL
  （Register Transfer Level，寄存器传输级代码，即 Verilog 源码）后，
  用 1600 万组随机输入 (a,b) 跑仿真，逐位算真实 MRED/MED（MED = Mean Error
  Distance，平均绝对误差；MRED 是它的相对值版本。circular-wrap 口径，
  即乘积回绕到 16 位比较）。MRED 是重尾分布（小乘积被截断会产生极大相对误差），
  必须 16M 组才收敛。**这是训练环里唯一的误差真值来源**（解析近似会系统性低估 0–30%）。
- **DC（面积/功耗代理）**：Synopsys Design Compiler，商业逻辑综合工具。把 RTL 综合到
  T28（台积电 28nm）工艺库，报面积（µm²）和一个功耗估计。DC 功耗有 ~15% 噪声，只用于环内相对比较。
- **XA（功耗终审）**：Synopsys XA（门级功耗仿真器）+ SAIF 活动文件（Switching
  Activity Interchange Format，记录每个信号翻转频率的文件，功耗仿真的输入），
  @1.5ns 测真实动态功耗。单 seed
  有 ±9% 噪声。**跨设计/跨臂的最终功耗结论一律以 XA 为准**，DC 功耗不作最终依据。

环内每个样本走 **verilator（误差）+ DC（面积/功耗代理）**；训练结束导出的前沿点再单独
跑 XA 终审。

---

## 3. 单个进程的初始化（跑第一个 episode 前）

1. 读 config + CLI（命令行参数），构造 `CompressorRouting` 对象。
2. 建 GCN + 三个类型头；类型头 bias（偏置项）偏向 exact（初始 P(选精确 cell)≈0.9，
   让策略从"近乎全精确"起步、按需再加近似 cell，避免冷启动一上来 85% 节点乱近似）。
3. `enable_pareto_v5(...)`：建 ParetoArchive（按 MRED 对数分箱），装种子队列（k 列表）。
4. `_start_reset`：建初始 Dadda 压缩树（Dadda 是教科书压缩树构造法），进初始池。
   —— v5 下这个池只是兜底，真正的初始种群是种子队列里的 Dadda 阶梯。

---

## 4. 一个 episode 的完整生命周期

每个 episode 分两种模式：**种子集**（前 13 个 episode，逐 k 把纯截断 Dadda 基线垫进
档案）和**进化集**（之后，从档案取亲代做变异搜索）。下面按 A→F 六步走，标出两种模式的分叉。

### Step A — `_v5_begin_episode`：本集编排

```
if 种子队列非空:
    k = 队列弹出下一个           # 种子集模式
    激活 k 的截断档（per-k 缓存 C*/误差floor，秒级 MC，每个 k 只算一次）
    state_override = 标准 Dadda 树 + 截断 k + 零 cell
    本集 bin = k 的 floor 落进的箱
else:
    本集 bin = episode_idx % 箱数    # 进化集模式：箱轮询
伪预算 = 本集 bin 的上沿
self.mred_budget = self.mred_scale = 伪预算   # 铰链无量纲化，跨箱同尺度
```

**关键点**：
- **k 是设计的属性，不是全局常量**（M0 k 线程化）。截断深度 k 决定：哪些低位部分积
  被当常数、校正常数 C\*、近似 cell 的资格列窗、以及误差下限。改 k **不动压缩树结构**
  ——截断列的压缩器照常实例化，靠 DC 综合的"常数传播"（输入恒 0 的门被工具自动删）
  扫成零成本。所以每个 k 的截断参数按需懒计算一次、缓存复用。
- **伪预算**：v5 里标量目标不决定存活，但 PPO（Proximal Policy Optimization，
  近端策略优化，约束"每步别改太猛"的主流强化学习算法）仍需一个标量奖励。
  取本集所在箱的上沿当"伪预算"，喂给旧的目标函数（误差铰链罚项
  `mred/伪预算 − 1`；铰链 = `max(0, x−门槛)`，门槛内零罚、超出线性罚）。`mred_scale=伪预算`
  使这个罚项无量纲、跨箱同尺度——消掉了逐箱调参的坑。

### Step B — `reset`：取亲代 + 变异

```
if 种子集: state = Dadda(k) 覆写           # 原样评估，不变异（这是可复现基线本体）
else:      state = 从本集 bin 均匀取一个档案亲代（箱空则最近非空箱）
激活 state["k"] 的截断档
if 种子集:  不变异
else:       _outer_mutate()               # 进化集才变异
```

`_outer_mutate` 分两层（k 随亲代继承，本战役 k 不跨档变异——M3 未做）：

1. **结构变异（每集必做）**：从合法动作集里选一个，`transition` 改压缩树结构
   （加/减一个 3:2 或 2:2 压缩器、FA↔HA（全加器↔半加器）互换、或 CT42
   （4:2 压缩器，一口气收 4 个同权输入）promote/demote）。
   结构搜索是主力——历史证据显示结构被饿会导致同误差下功耗劣化 16%。
2. **cell 叠加层（骰子四选一）**：
   - `keep`：cell 维度不动；
   - `cell`：加/删/换一个近似 cell（解析提议 + 闭式可行性过滤；闭式 =
     closed-form，有现成公式直接算、不用仿真）；
   - `resample-K`：清空重摆 K 个 cell（K 从基数集 {0,1,2,4,8,16,32,64} 采）；
   - `zero`（**M2 新增**）：`zero-col` 把最低的未清列**整列**填 ZERO cell = 分数截断
     一步（ZERO cell 输出恒零 = 把这一列的部分积整个丢掉，是 k 和 k+2 之间的连续插值），
     或 `unzero-col` 反向撤一列。**关键：zero 算子跳过闭式预算过滤**——解析模型对边界
     列 ZERO 的偏置估计失真 3.7 倍，一阶过滤会把密集 ZERO 包在提议阶段就误杀
     （这正是旧 GA 最多放 ~7 个 cell、而离线贪心同预算能放 25–77 个的机制根源）。
     可行性改交给 Step C 的 TT oracle 和 Step D 的档案支配裁决。

### Step C — `get_samples`：采样、发射、oracle、DC

1. GCN 前向：把当前压缩树编码成图，输出每个节点的嵌入 → routing logits（布线概率）。
2. 采 **32 个布线样本**：从 logits 采样每个压缩器的连接方式（同一集内 32 个样本
   **共享同一个变异后结构**，只有布线不同——所以 PPO 的信用分配只含布线，cell/结构
   由外环进化决定）。每个样本发射成 RTL 文件（带上 cell_map 和截断信息）。
3. **sample-0 过 TT oracle 预筛（M2）**（TT = truth table 真值表；oracle =
   "神谕"，泛指能快速给出准确判定的检验器）：`_outer_tt_oracle_screen`——用
   cellsolver 的张量化仿真器（TreeSim）在**真实布线**上实测本集 cell 配置的 MRED（与 16M verilator
   闸门同流、逐位一致，但秒级）。
   - 上限：v5 下 = **档案 MRED 上限**（不是伪预算！超伪预算的样本只是会落进更松的箱去
     竞争，不算废样本——这是 v5 相对旧机制的核心改动，旧机制会把超预算样本整集报废）；
   - 若实测超上限：按解析贡献 wae·2^col 降序**二分前缀摘除** cell，直到落回上限内，
     重发射 sample-0 的 RTL；后续样本自然沿用修剪后的配置。
   - oracle 说可行 ⇒ 16M 闸门必过（构造性保证），密集 ZERO 包不再被误杀。
4. **注入 all-exact 保底候选**：额外发射一个同布线、全精确 cell 的设计，只参与档案/best，
   不进 PPO——防止 cell 采样把最优点拖得比纯截断还差。
5. **派发 DC**：33 个 RTL（32 样本 + 1 保底）送 32 路 DC worker；每个 worker 内部
   **同时**跑 verilator 16M 测误差（~3s，远快于 DC ~200s，不拖慢）。
6. 收结果：DC 失败的样本**直接踢出本批**（不致命、不污染 PPO/档案）；verilator 失败的
   样本记 mred=None（Step D 不入箱）。

### Step D — `_v5_admit_samples`：支配准入（v5 的心脏）

替代旧的"标量 rank 选一个 best"。每个拿到实测 MRED 的样本：

```
bin = MRED 落进的对数箱（超档案上限 → 丢弃）
在该箱内做 (面积, 功耗) 二维支配准入：
  - 若被箱内已有解支配 → 拒收
  - 若与已有解在 ε_power(=1%) 内重复 → 拒收
  - 否则收入，并剔除被它支配的旧解
  - 箱内超 bin_cap(=8) → 按 NSGA-II 拥挤度淘汰（NSGA-II = 经典多目标遗传算法，
    这里只借它的拥挤度公式；拥挤度 = 解周围邻居的密集程度。挤在一起的先删，
    两端极值保留）
```

**零人工系数**：一个解只要在自己的误差箱内不被别人全面压制就存活。唯一的数值参数
ε_power=1% 不是偏好权重，是 DC 复跑的噪声地板（功耗差小于 1% 视为测量分辨率内相同，
由面积裁决）。**支配 = 法官，标量目标 = 参谋**——这就是"系数没依据会丢解"的解法：
加权和只能取到前沿凸包（convex hull，包住整个点集的最小凸边界）上的点，前沿
凹陷区的解被任何正权重系统性丢弃；支配准入不丢。

### Step E — PPO 更新（参谋角色）

用 `get_objective` 算每个样本的标量奖励（误差铰链 + 面积/功耗加权和，本战役
权重 delay/area/power = 2/2/1），advantage（优势 = 该样本比本批平均好多少，
决定梯度方向与力度）归一化，跑 1 个 PPO 梯度 epoch（一轮完整梯度更新）更新
GCN + 类型头。具体公式、clip=0.2、辅助 loss 等见 `V5_EPISODE_LIFECYCLE.md` §5。**注意**：这个标量**只影响 PPO 往哪个方向
提议布线**（学习贡献已裁决约 1%），**不再决定谁存活**——任何被采出的样本都按实测
(MRED, 面积, 功耗) 走 Step D 的支配准入，好解不会因为标量分低被扔。

### Step F — 落盘

- 每 `save_freq`（20）集：存 GCN + 类型头权重、best_info.json、**front.json**（档案快照：
  每个非支配解的 {箱, MRED, 面积, 功耗, k, cell 数}）；log_dir 根下滚动更新
  `front_state.json`（含 payload 的全量档案，原子写，崩溃后可收割）。
- 每 `front_dump_freq`（5）集：`logs/front_hist/front_epNNNN.json` 轻量快照
  （KB 级，2026-07-14 新增，专供回放可视化；当前 r2 进程尚未部署此改动，
  其颗粒度仍是 20 集）。
- **回放查看器**：`python scripts/gen_v5_front_viewer.py <run目录>
  [--compare greedy=<对照目录>]` 生成单文件 HTML，拖动进度条逐 ep 看
  area/power vs MRED 前沿演化（含纯截断 Dadda/arith 基线背景）；
  远端数据用 `bash scripts/sync_v5_fronts.sh <远端run>` 秒级增量同步。
- 训练结束：`export_front` 把整个档案导成 `front/k{kk}_bin{NN}_{j}/` 目录，每个含
  `MUL.v` + `best_info.json`——目录名以 `k` 开头，**兼容现有 reeval 管线的 k\* glob**，
  XA 终审零新工具。

---

## 5. 三个搜索维度如何协同

| 维度 | 谁在推 | 机制 |
|---|---|---|
| **误差轴（MRED）** | 种子阶梯 + zero-col + k 继承 | 不同 k 截断张开粗网格，zero-col 在相邻 k 之间做连续插值填空隙 |
| **面积轴** | 结构变异 + 近似 cell | 压缩树结构搜索 + substd cell 换下标准单元 |
| **功耗轴** | 真实 DC/XA 测量揭示 | 功耗**不进**优化器打分（cell 级功耗噪声大、方向不可靠，已裁决）；靠档案在 (面积,功耗) 平面留下所有非支配点被动揭示 |

误差轴的粗细两级：**k = 粗档**（每 +2 一跳），**zero-col = 细插值**（同 k 内一列一列填
ZERO，把误差从 k 的 floor 连续推到接近 k+2）。两段进程的 k 种子范围重叠，合并时
seg_lo 的"k12 + 密集 cell"会和 seg_hi 的"k16 纯截断"在同一误差区对撞，谁面积功耗更优谁进最终前沿。

### 5.1 "种群"到底是什么

v5 没有传统 GA 的固定大小种群，分三层看：

| 层次 | 大小（本战役实测） |
|---|---|
| 每集评估的"子代" | 32 个采样 + 1 个 all-exact 保底 = 33 个/集 |
| 档案容量上限（存续种群） | seg_lo 112 + seg_hi 80 = 192 |
| 实际稳态占用 | ep400 时两段合计约 36 条 |

繁殖 = 每集按目标箱从档案取一个亲代变异采样，所以"有效种群"就是档案里那几十条
非支配解；回放查看器每一帧的点数即当时的种群规模。

### 5.2 算法定位：不是 NSGA-II

v5 只借用了 NSGA-II 的**拥挤度公式**（且几乎不触发），主体差别很大：

| | 标准 NSGA-II | v5 |
|---|---|---|
| 种群 | 固定 N，每代整体换血 | 分箱档案，持续维护 |
| 保留谁 | 非支配排序分层填满 N | 只留箱内非支配（相当于只要第 1 层） |
| 新解来源 | 锦标赛选择 + 交叉 + 变异 | GCN 策略采样（PPO 训练），变异算子本身在学习 |
| 多样性 | 拥挤度 | MRED 对数分箱强制全误差段覆盖 |

准确的一句话描述：**MAP-Elites 风格的分箱 Pareto 档案（ε-支配准入）+ RL 学习的
生成算子**（MAP-Elites = 质量-多样性算法：按特征分格子、每格只留最好的，保证
整条谱都有解）。写论文别直接叫 NSGA-II。

---

## 6. 收尾与验收

训练完两段各得一个档案（front/ 目录）。合并流程：
1. 两段 front/ 合并，跑 **XA 终审**（真实动态功耗）。
2. 取全域非支配集 = 最终交付的帕累托前沿。
3. **验收判据**（等误差 XA 口径）：
   - ① 前沿包络 ≤ 纯截断 Dadda C\* 基线（种子即基线，构造性保证不劣，重点看空隙区压出多少）；
   - ② 前沿包络 vs 离线 budget_sweep 25 点 + warm240eg（真实对打：冷启动能否
     追平带温启动的历史最强；温启动 = 用历史 run 的最优解做初始种群，冷启动 =
     只用教科书构造从零开始）；
   - ③ **多样性审计**：档案里存在"旧标量口径会淘汰、支配口径保住"的点（同箱面积更大但功耗更小的非支配解）——这是"系数会丢解"的直接证据。

---

## 7. 当前实时状态（2026-07-14 晚更新）

- r2 两段（2026-07-13 12:56 起跑）健康推进：seg_hi 已过 **ep420**、seg_lo
  **ep380**（目标 480），DC/verilator 无系统性失败；greedy A/B 对照
  （`2026-07-13_v5r2_greedy`，seg_hi 单段）已到 ep180。
- 种子基线精确落位（k2 ≈1009µm²、k12 =742µm² 等，n_approx=0 分毫不差）；
  进化集已大量触发 zero-col / TT oracle。
- ep420 观感（环内 DC 口径，非终审）：面积轴 GA 合并前沿贴着纯截断基线略优；
  功耗轴在 3e-3~1e-2 区间拉开明显优势；GA vs greedy 未分出全面胜负
  （greedy 1e-2~1e-1 略优、GA 3e-3 附近略优）。
- 回放查看器已上线：`outputs/2026-07-13_v5r2_np2/front_viewer.html`
  （橙=GA 合并前沿、黄=greedy、灰/紫=纯截断基线，拖动进度条逐 ep 回放）。
- 待办：`front_dump_freq=5` 代码已就绪未部署（部署需重启进程，r2 跑完后随
  下一个 run 生效）；两段跑完后合并 front/ + XA 终审出最终前沿。

---

## 附：这次相对旧单目标 GA 的三处根本改动

1. **单点预算 → 全轴档案**：旧版一次 run 只优化一个 MRED 预算下的单目标（面积×权重+
   功耗×权重），产出 1 个点；v5 一次 run 产出整条前沿，且选择层无权重。
2. **超预算废样本 → 落箱竞争**：旧 errgate（error gate，误差闸门：DC 前先测误差、
   超标就拦下的预筛机制）把超预算样本整集报废（历史 25–64% episode
   全员超标白跑 DC）；v5 里超伪预算只是落进更松的箱，EDA 一分不白花。
3. **cell 生成瓶颈解除**：旧一阶闭式过滤误杀密集 ZERO 包（GA ≤7 cell）；M2 的 zero-col
   批量算子 + TT oracle 实测放行让 GA 能自发堆出几十个 ZERO（e2e 已复现 −4.4% 面积胜点）。
