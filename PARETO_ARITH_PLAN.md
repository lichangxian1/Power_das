# arith 多目标化方案 v5：混合截断种群 + 非支配档案

**状态：M0+M1+M2 已实现并冒烟通过（07-12/13，用户定名 v5、批准动工；M2 应用户
"先补 M2"指令实施）。** M2 落地细节：`--outer_zero_ops`（zero-col 一步整列填 ZERO，
冒烟实测单步 +11 个 cell vs GA 历史整包 ≤7，逐列爬升=分数截断阶梯；旁路闭式过滤）、
`--outer_tt_oracle`（复用 GradientCellSolver/TreeSim 在 sample-0 布线实测 mred，
替代 verilator 预筛；超限按解析贡献二分前缀摘除；v5 下限档=档案上限而非伪预算——
连带修正 `_gate_budget_exceeded` 的 v5 语义：超伪预算=落松箱竞争≠废样本）、
`_EXACT_AREA_INCTX` 锚点（提议打分 exact 面积 2.856/2.184/5.712 对齐 cellsolver
07-11 口径）、`--approx_cardinality_choices` CLI 化（v5 战役建议 0,1,2,4,8,16,32,64）。
M3（k± 宏变异）未动。真 DC e2e×2 通过：①落松箱语义（超伪预算样本正常入箱，
旧机制下整集报废）；②zero+oracle 全链路，产出首个进化胜点——7 cell 包
在 k8 同误差箱内支配淘汰纯截断种子（877.3µm²/11.74mW vs 917.3/12.26，
面积 −4.4%/功耗 −4.2%）。**表述修正（07-13 双评审）**：该包实为 7 个零偏置
substd cell（当时菜单无恒零 cell 且 group=='Z' 检测有误），胜点真实但不是
分数截断；恒零 cell 已于 07-13 注入菜单（add_zero_cells_unified.py +
const_zero 标志），r2 战役起 zero-col 才是真·分数截断。教训：oracle 冒烟用
2M 小池 mred 估计偏差 ~56%，正式战役必须 16M 池（默认值）。演进：v1（预算循环）
→ v2（按用户依据重构为支配档案）→ v2.1（Dadda 种群）→ **定名 v5 开工**。

**实现落点**：`utils/common.py::ParetoArchive`（分箱+支配准入+拥挤淘汰，选择层零系数）；
`trainer/arith_das.py`（`enable_pareto_v5` / `_activate_trunc_profile` per-k 档缓存 /
`_v5_begin_episode` 种子集+箱轮询 / reset 亲代继承 k / `_v5_admit_samples` 支配准入 /
`found_best_info` property 路由 / `export_front`）；`scripts/train_dc.py --pareto_v5 …`；
冒烟 `scripts/smoke_pareto_v5.py`（36 断言全过）+ 旧集成冒烟回归过 + 1-ep 真 DC 端到端。
全部挂 `--pareto_v5` 门：不传 = 逐位旧行为；M2 各件同理独立开关。M3（k± 宏变异）未动。

## 0. 设计依据（用户 07-12 口述，方案的宪法）

> 多了误差指标后，本身做的就是多目标优化问题。手工给不同目标设系数没有依据；
> 用多目标优化得到很多帕累托解，解空间大、更有多样性，不会因为系数问题忽略好的解。
> 单目标改多目标，**不同截断可以作为初始种群**。

由此定三条设计原则：
- **P1 选择层零人工系数**：谁存活、谁当亲代、谁进报告——只由支配关系决定，
  不经过任何 `w_area·area + w_power·power` 式的标量。
- **P2 误差全轴覆盖**：MRED 是坐标轴不是约束，档案跨 1e-7~2e-1 全程维持多样性。
- **P3 混合 k 种群**：不同截断深度的设计同池竞争，纯截断阶梯 k02~k30 作初始种群。

## 1. 现状系数审计：哪些系数真的在丢解（本次核对代码坐实）

| 系数 | 位置 | 在丢解吗 |
|---|---|---|
| `area_weight=2.0 / power_weight=1.84` | obj（`arith_das.py:3457-3464`）、**best 选择 `_candidate_rank:3476`、池准入** | **在丢**。注释自认"等权"是拍的；且 Q1 已裁决环内功耗系数无依据（DC 代理噪声 15%）。加权和只能取到前沿凸包上的点——area-power 平面的非凸凹陷区，任何正权重组合都够不着，这类解被系统性静默淘汰 |
| `med_violation_weight=1.0` 铰链 | obj `:3404` | 半丢：决定超预算样本被推回的力度，间接影响探索 |
| `mred_budget`（ε-约束） | rank 分层 `:3480` | 不丢凸包意义上的解（ε-约束扫描可达非凸区），但**单点 ε 一次只看一个误差水平**，档位间隙的解全部不可见（budget_sweep 已证空隙里有 −17~−20%） |
| delay 约束 | `:3441` | 不算系数：1.5ns 是硬规格，保留为约束 |

结论：v1（预算循环）解决了"单点 ε"但保留了选择层的 PPA 标量——不满足 P1，降级。
v2 把支配准入放进选择层，系数只允许残留在"提议启发"层（见 §2.4，那里不丢解）。

## 2. 形态 v2：混合 k 种群 + mred ε-分箱非支配档案

### 2.1 档案（population = archive）
- 结构：MRED 轴按对数分箱（×2 间距，1e-7~2e-1 约 21 箱）——分箱即 mred 轴的
  **ε-支配网格**，同时承担容量控制与 P2 的覆盖保证。
- 箱内：(area, power) **二维支配准入**，无任何权重。容量上限每箱 ~6，超限按
  拥挤度淘汰（挤在一起的先删，保多样性）。
- 功耗轴量化 ε_power ≈ 1%（DC 复跑噪声地板）：差距小于测量分辨率视为同值、
  由面积裁决。这不是偏好系数，是仪器分辨率，有实测依据（Q1/DC 1% 地板）。
- 全档案（~21 箱 × ≤6）就是交付物本体；`found_best_info` 退化为"箱代表"
  （箱内最小面积成员），仅供日志/reset 兼容——49 处现有引用经 property 路由不改。

### 2.2 初始种群 = 不同 k 截断的 Dadda 树（P3 + 可复现性约束，用户 07-12 裁定）
- **种子 = {标准 Dadda 树 + 截断 k + mred-C\*} for k ∈ {2,3,...,30}**，全部从零
  确定性构造：Dadda 是教科书构造（`CompressorTree.dadda` 已有）、截断+C\* 是
  固定 seed 的确定性计算（`_setup_truncation`）、零近似 cell。**不依赖任何历史
  run 产物**——论文读者按文中规格即可逐位重建初始种群。
- 启动播种评估：第 0 集前把 ~29 个种子走标准管线（DC + verilator 实测 mred）
  拿到真实 (mred, area, power) 入箱，成本 ≈ 一两集的 EDA 批量，一次性。
- 双重身份：初始种群同时就是**纯截断 Dadda C\* 基线**——基线与起点重合，
  前沿相对初始档案移动了多少 = 搜索的净贡献，归因无懈可击（审稿人无法质疑
  "前沿是温启动偷带进来的"）。
- 历史成果种子（warm240eg/budget_sweep 25 点/deepk）降级为**可选非论文臂**
  （`--init_pool_best_info` glob 保留），只作内部上界参照，正式战役不用。
- 前提=**k 线程化**（M0）：`state["k"]` + per-k TruncProfile lazy 缓存。可行性已核实
  （原方案三的三大 blocker 全部消解，论证保留在 §6）。
- 复现性边界（论文如实声明）：搜索侧全确定（seed42、确定性调度、固定 seed C\*/MC）；
  DC/XA 是商业工具+T28 库，复现需同版本工具链——领域惯例，规格全列出即可。

### 2.3 亲代选择与变异
- 亲代：箱轮询 + 箱内均匀采样（拥挤度加权可作 v2.1）——每集从档案取一个亲代，
  k 随亲代继承。确定性调度，可断点续训。
- 变异三层（同 v1 §7 设计）：
  - 宏 `k±1/±2`（M3，~10%/集）：换 per-k profile + 剪死列 cell，零结构修复；
  - 中 `zero-col / unzero-col`（M2）：整列填/撤 ZERO = 分数截断，档间空隙的主力；
  - 微：v1.1 现有 per-slot add/remove/swap/resample-K 不动。
- ZERO 打分命门修复（M2）：cellsolver TT 张量化仿真器 + S3 分层估计器进
  `_cells_budget_ok` 变异回路——位精确、秒级、与 16M 闸门同流；修掉边界列解析
  bias 3.7× 失真与一阶 slack 对密集包的误杀。附带检查：`_propose_cell_add`
  面积打分对齐原生锚点口径（cellsolver 侧已切，trainer 侧疑似仍 standalone 标价）。

### 2.4 PPO 的位置：系数退到"提议启发"层（不丢解的层）
- PPO 每集报酬仍需标量：取**亲代所在箱的箱上沿为伪预算**，沿用现有
  `get_objective`（铰链 `mred/伪预算−1` 无量纲化）。改动最小。
- 为什么这不违反 P1：标量只影响 PPO 往哪儿提议（学习贡献已裁决 ~1%），
  **不再决定谁存活**——任何被采出的样本都直接按实测 (mred, area, power) 走支配
  准入，好解不会因为标量分低而被扔。系数从"法官"降为"参谋"。
- 备选（v2.1 观察项）：reward = 档案 hypervolume 增量（`paretoset`/`hypervolume`
  已在依赖里，`:25/:2175`）。先不做——为 ~1% 的引擎重设计 reward 不划算。

### 2.5 errgate/鲁棒修复的简化红利
支配准入下**"超预算废样本"概念消失**：mred 超出亲代箱的样本只是落进更松的箱去
竞争，EDA 不白花（外环三病之一自动痊愈）。门只挡两类真浪费：mred > 全域上限
2e-1（无箱可落）、verilator 失败。鲁棒修复（8 布线合规摘 cell）改为对全域上限
兜底，不再对箱上沿紧修。

### 2.6 进程划分（np4，无共享内存约束下的并行）
进程间不共享档案（文件同步复杂度不值），改为**误差段进程**：
P1 覆盖 1e-7~3e-5（种子 k02-k08）、P2 1e-5~1e-3（k06-k12）、P3 3e-4~2e-2
（k10-k18）、P4 6e-3~2e-1（k14-k24）；相邻重叠 ≥2 箱，合并时对撞校验。
段内 k 自由（继承+宏变异），段就是该进程档案的箱范围。

## 3. 代码改动（里程碑制，每个独立开关独立验收）

| 里程碑 | 内容 | 开关 | 量 |
|---|---|---|---|
| **M0 k 线程化** | `state["k"]`；`_trunc_profiles={k:(bits,C*,floor)}` lazy 缓存（`_setup_truncation:1889` 守卫改造）；emission/export 读 per-state k（`:1799-1801,:2019,:3526`）；资格窗/`pow2k` scale 随 k（`:1236,:1542`） | 隐式（旧 config 单 k 行为不变） | ~150 行 |
| **M1 支配档案** | 分箱档案类（准入/淘汰/拥挤度）；`found_best_info`/`pool` property 路由；亲代轮询调度；多源初始种群装载（`_seed_pool_from_best_info:541` 扩多文件+按 mred 归箱）；`front.json`+`front/` 导出（兼容 reeval_xa_glob_tmpbuild） | `--pareto_archive` | ~250 行 |
| **M2 ZERO 强化** | TT oracle 进变异回路；zero-col/unzero-col 算子；锚点口径检查 | `--outer_zero_ops` | ~200 行 |
| **M3 k 宏变异** | k±1/±2 算子 + 死列 cell 剪除 + 每箱 k 多样性配额 | `--outer_k_mutate` | ~80 行 |

依赖：M1 依赖 M0；M2/M3 依赖 M1，彼此独立。M0+M1 = 最小可跑的多目标战役
（k 只继承不变异，也已满足 P1/P2/P3——k 多样性来自初始种群）。
PPO 侧零改动（伪预算复用 get_objective）。合计 ~680 行 + 冒烟。

## 4. 战役设计（launch 前逐项确认）

- 配方基线 = warm240eg 镜像：GA v1.1、errgate verilator、window6/max_col30、
  菜单 `selected_compressors_all_substd.json`、ep240、seed42、np4（gpu2）、
  TZ=Asia/Shanghai、贪心不进环（07-12 裁决）。
- 初始种群（launch 时点名核对）：**仅** 截断 Dadda 阶梯 k∈{2..30}（§2.2，~29 种子，
  按启动播种评估的 measured mred 归箱）；历史成果臂另开、明确标注非论文。
- **验收（XA 口径，合并 4 进程档案后取全域非支配集）**：
  ① 等误差包络 ≤ 初始种群阶梯（= 纯截断 Dadda C\* 基线；准入含种子 ⇒ 构造性
    保证不劣，看的是压出多少、尤其档间空隙区）；
  ② 等误差包络对比 budget_sweep 离线 25 点与 warm240eg（**真实对打**，不再有
    构造性保证——冷启动 240 集能否追平/超越带温启动的历史最强，是论文主张
    "该方法从零可复现地达到 SOTA"的直接检验）；
  ③ 等误差包络 ≤ mred-C\* arith 纯截断 RL 基线（族内最强 baseline）；
  ④ **多样性审计**（P1 的直接检验）：档案里存在 v1 标量口径下会被淘汰的点
    ——同箱内 area 更大但 power 更小的非支配成员数 > 0，即"系数会丢、支配保住"的解。
- MED/WCE：不设约束，front.json 全程记录，验收审计（zero-col 有 MRED 便宜
  MED 贵的套利风险，数据说话再议副约束）。

## 5. 风险

| 风险 | 缓解 |
|---|---|
| DC 代理功耗噪声制造伪非支配点（档案膨胀） | ε_power=1% 量化 + 每箱容量 6 + XA 终审剪枝 |
| 深 k 在松箱天然强势，浅 k+密集 cell 血统灭绝 | 每箱 k 多样性配额（M3）；M1 期 k 只继承，血统由初始种群保底 |
| 每箱有效集数少（240 集/21 箱） | 段进程只养自己的 ~7 箱；箱轮询保证最低访问额 |
| **冷启动追不上历史温启动成果**（复现性的代价） | 种子即基线故绝不劣于纯截断；若 240 集追不平 warm240eg，可加集数（ep360/480）或多 seed 并行取并——都不破坏可复现性（规格照列）；历史臂仅作内部参照 |
| PPO 伪预算逐集跳变扰动 advantage 归一化 | 铰链无量纲化后跨箱同尺度；冒烟专项验证；兜底 lr=0（引擎本就是进化棘轮） |
| 断点续训 | 档案全量进 checkpoint；调度确定性重放 |
| 老坑 | launch 检查表：PATH vtool / OMP / setsid / max_col=30 注入 / 42 菜单 max_types |

冒烟：① 旧 config 无开关 → 逐位旧行为；② 档案准入/淘汰单元测试（构造支配/非支配/
同 power ε 内案例）；③ 混合 k 种群装载后 reset/emission 正确（每 k 抽一设计
RTL vs TT 仿真 Δmed=0）；④ front/ 被 reeval 管线吃进；⑤ 续训对齐。

## 6. 附：方案三 blocker 消解论证（07-12 核实，v1 §7 保留）

1. **k 变异零结构修复**：截断列压缩器本就实例化、DC 常数传播扫成 0 成本
   （liveXX 回归 R²=0.997 证实）；树恒定义在全 31 列，k 只换
   (常数 PP, C\*, 资格窗, floor)。
2. **k 是 per-episode 状态**：PPO 只采布线，一集共享一个结构 → reset 换档即可，
   `_setup_truncation` 的单 k 守卫（`not self._trunc_bits`）改 per-k lazy 缓存，
   C\* 全宽 MC 秒级/档、确定性 seed、全 run 摊销 ~15 次。
3. **跨 k 可比性**：选择层支配化后 k 不进任何尺度；环内每集真实 DC ⇒ spike
   "跨 k 排序必须 DC 探针"构造性满足，加法代理幻觉无处藏身。

## 7. v1→v2 变更记录

- **v2.1（07-12，用户裁定）**：初始种群改为**仅不同 k 截断的 Dadda 树**（从零确定性
  可构造，保证论文可复现）；历史成果种子（warm240eg/离线 25 点）降为可选非论文臂；
  验收 ② 从构造性保证改为冷启动 vs 历史温启动的真实对打。

- 预算循环从"形态本体"降为"PPO 伪预算 + 分箱骨架"（§2.4/2.1）——v1 保留了
  选择层 PPA 标量与铰链法官地位，不满足 P1（系数丢解），被用户依据否决。
- k 变异从二期可选升为 M0/M3——混合 k 初始种群（P3）是一期需求。
- 紧→松级联播种删除——支配准入 + 全量初始种群使其冗余。
- 超预算废样本/鲁棒修复紧修删除——支配准入下样本落箱即竞争（§2.5）。
